# Typechecking

- After work is done, validate by running `cargo check` instead of `cargo build` except explicity told to.
- When you make a change to .proto files, the updated types need to be regenerated by tonic-prost in the build process so you need to run `cargo build` for that.
